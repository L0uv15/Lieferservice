export const HOME_ROOT = "/" as const;
export const HOME_BASE = "" as const;

export const HOME_ROOT_PATH = `${HOME_ROOT}` as const;
export const HOME_BASE_PATH = `${HOME_ROOT}/${HOME_BASE}` as const;