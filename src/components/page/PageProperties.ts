import React from "react";

type PageProperties = {
    navExtensionNode?: React.ReactNode,
    pageHeadExtensionNode?: React.ReactNode,

    title?: string,
    documentTitle?: string,
}

export default PageProperties;